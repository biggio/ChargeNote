{
  "name": "chargenote",
  "version": "1.0.0",
  "description": "Cloudflare Worker for ChargeNote (電記) D1 sync",
  "main": "worker.js",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
