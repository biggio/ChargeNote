{
  "name": "ev-charger-sync",
  "version": "1.0.0",
  "description": "Cloudflare Worker for EV Charger Recorder D1 sync",
  "main": "worker.js",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
